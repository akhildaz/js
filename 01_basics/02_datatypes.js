// "use strict";  treats all js code as newer version

let name = "akhil"; // type string
let age = 18; // type number
let isLoggedIn = true; // type boolean

let city = null // null -> standalone value
let state; // this variable not defined during declaration is the value will be undefined until someone define it

console.log(typeof state);
